
<p id="notice"><%= notice %></p>
  <div class="row">
    <div class="col-md-6">
      <p>
        <strong>Name:</strong>
        <%= @product.name %>
      </p>
      

      <p>
        <strong>Description:</strong>
        <%= @product.description %>
      </p>

      <p>
        <strong>Image url:</strong>
        <%= image_tag(@product.image_url, class: 'img-responsive') %>
      </p>

      <p>
      	<strong>Colour:</strong>
      	<%= @product.colour %>
      </p>

      <p>
      	<strong>Price:</strong>
      	<%= @product.price %>
      </p>
      <p>Average Rating: 
        <div class="rating" data-score=<%= @product.average_rating %>></div>
       </p>
    </div>
 
      <div class="col-md-6">
        <%= render "new_comment" %>
        <%= render "comments" %>
        
      </div>

      <%= link_to 'Edit', edit_product_path(@product), class:'btn vtn-primary' %> |
      <%= link_to 'Back', products_path, class:'btn btn-primary' %>

   </div>

